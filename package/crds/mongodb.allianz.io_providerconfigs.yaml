---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: providerconfigs.mongodb.allianz.io
spec:
  group: mongodb.allianz.io
  names:
    categories:
    - crossplane
    - provider
    - mongodb
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.credentials.aws.secretsManager.secretName
      name: SECRET-NAME
      type: string
    - jsonPath: .spec.credentials.aws.secretsManager.region
      name: REGION
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          A ProviderConfig configures the MongoDB provider with AWS-only credential storage
          ELIMINATES ALL Kubernetes secret references
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A ProviderConfigSpec defines the desired state of a ProviderConfig.
            properties:
              credentials:
                description: |-
                  Credentials required to authenticate with MongoDB Atlas API
                  Stored ONLY in AWS Secrets Manager - NO Kubernetes secrets
                properties:
                  aws:
                    description: AWS Secrets Manager source - NO Kubernetes secrets
                      allowed
                    properties:
                      secretsManager:
                        description: AWS Secrets Manager configuration for provider
                          credentials
                        properties:
                          kmsKeyId:
                            description: AWS KMS Key ID for decryption (optional)
                            type: string
                          region:
                            description: AWS Region where the provider credentials
                              secret is stored
                            type: string
                          secretKey:
                            default: apiKey
                            description: 'Secret key within the JSON secret (default:
                              "apiKey")'
                            type: string
                          secretName:
                            description: AWS Secrets Manager secret name or ARN containing
                              MongoDB Atlas API key
                            type: string
                        required:
                        - region
                        - secretName
                        type: object
                    required:
                    - secretsManager
                    type: object
                  source:
                    allOf:
                    - enum:
                      - AWS
                    - enum:
                      - AWS
                    description: Source of the provider credentials - only AWS sources
                      supported
                    type: string
                required:
                - source
                type: object
            required:
            - credentials
            type: object
          status:
            description: A ProviderConfigStatus reflects the observed state of a ProviderConfig.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
